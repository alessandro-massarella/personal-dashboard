import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

// https://vite.dev/config/
export default defineConfig({
  plugins: [vue()],
  base: '/personal-dashboard/', // Assumes the GitHub repository will be named "personal-dashboard"
})
